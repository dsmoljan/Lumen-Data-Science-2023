defaults:
  - default.yaml

train_dataloader:
  batch_size: 1
  collate_fn: src.data_utils.data_utils.collate_fn_windows_stack

val_dataloader:
  batch_size: 1
  collate_fn: src.data_utils.data_utils.collate_fn_windows

test_dataloader:
  batch_size: 1
  collate_fn: src.data_utils.data_utils.collate_fn_windows

train_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

val_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

test_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

general:
  # IRMAS
  log_mel_spectrogram_mean: -4.2677393
  log_mel_spectrogram_std: 4.5689974
  # AudioSet
  # log_mel_spectrogram_mean: -0.073369932
  # log_mel_spectrogram_std: 0.394402992