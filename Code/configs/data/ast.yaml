defaults:
  - default.yaml

train_dataloader:
  batch_size: 1
  collate_fn: src.data_utils.data_utils.collate_fn_windows_stack

val_dataloader:
  batch_size: 1

test_dataloader:
  batch_size: 1

train_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

val_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

test_dataset:
  sr: 16000
  return_type: audio
  use_window: true
  window_size: 1

train_augmentations:
  audio:
    active: False
    add_noise:
      active: False
    pitch_shift:
      active: False
    time_shift:
      active: False

general:
  # IRMAS
  log_mel_spectrogram_mean: 0.326588890
  log_mel_spectrogram_std: 0.287198263
  # AudioSet
  # log_mel_spectrogram_mean: -0.073369932
  # log_mel_spectrogram_std: 0.394402992